# Mihomo Rulesets

Reproducible Mihomo MRS artifacts generated from canonical upstream text
rulesets without changing their rule coverage.

## Published artifacts

Stable rolling-release URLs:

```text
https://github.com/skyswordw/mihomo-rulesets/releases/download/rolling/reject_domainset.mrs
https://github.com/skyswordw/mihomo-rulesets/releases/download/rolling/china_ip.mrs
https://github.com/skyswordw/mihomo-rulesets/releases/download/rolling/download_domainset.mrs
https://github.com/skyswordw/mihomo-rulesets/releases/download/rolling/apple_cdn.mrs
```

Mihomo provider example:

```yaml
rule-providers:
  reject_domainset:
    type: http
    behavior: domain
    format: mrs
    url: https://github.com/skyswordw/mihomo-rulesets/releases/download/rolling/reject_domainset.mrs
    path: ./rule_set/sukkaw_ruleset/reject_domainset.mrs
    interval: 43200
```

MRS is a Mihomo/Clash.Meta format. Original Dreamacro Clash, sing-box, and
Surge require their own rule formats.

## Automation

GitHub Actions checks every source declared in `sources.json` every six hours.
An update is published only after all of these checks pass for every ruleset:

- minimum source entry and byte counts;
- pinned Mihomo archive checksum;
- deterministic conversion from the same source;
- minimum artifact size;
- Mihomo configuration validation.

If any check fails, the existing rolling artifact remains unchanged. Metadata
and SHA-256 checksums are published beside the MRS file.

To regenerate locally:

```bash
MIHOMO_BIN=/path/to/mihomo ./scripts/update-rulesets.sh
```

## Source and license

The canonical source URLs, conversion behaviors, and declared licenses are in
`sources.json`. The Sukkaw sources are generated by
[`SukkaW/Surge`](https://github.com/SukkaW/Surge) under AGPL-3.0. `china_ip`
comes from the source used by the existing Sub-Store profile; that source
currently declares no license, so its metadata uses `NOASSERTION` and this
repository grants no additional rights over that source data.

The automation code in this repository is distributed under AGPL-3.0. See
`LICENSE` and each generated metadata file for provenance and source-specific
terms.
